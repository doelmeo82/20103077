import 'package:flutter/material.dart';

class HoTroSVMenu extends StatelessWidget {
  const HoTroSVMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Hỗ trợ sinh viên',
        style: TextStyle(fontSize: 40),
      ),
    );
  }
}
